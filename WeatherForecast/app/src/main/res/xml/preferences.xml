<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <SwitchPreference
        android:key="pref_connection_state"
        android:title="@string/pref_connectionType"
        android:defaultValue="true" />

    <ListPreference
        android:key="pref_forecastDays"
        android:title="@string/pref_forecastDays"
        android:dialogTitle="@string/pref_forecastDays"
        android:entries="@array/pref_forecastDays_entries"
        android:entryValues="@array/pref_forecastDays_values"
        android:defaultValue="10" />
</PreferenceScreen>